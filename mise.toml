[tools]
bun = "1.3.9"

[tasks.build]
description = "Bundle rebot into dist/rebot.js (requires Bun at runtime)"
run = "bun run build"

[tasks.compile]
description = "Compile rebot into a standalone binary (~100MB, embeds the Bun runtime)"
run = "bun run build:binary"
